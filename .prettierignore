# Generated by release-plz from the conventional-commit history and never
# hand-edited (AGENTS.md) — reformatting it would fight the next release.
CHANGELOG.md
**/CHANGELOG.md

# Generated by `mise run token-bench` from the committed fixtures and diffed
# byte-for-byte by `mise run token-bench-check` (CLOUD-119) — reformatting it would
# fail the gate on the next run, exactly as CHANGELOG.md would fight release-plz.
bench/tokens/RESULTS.md

# `bench/suites/RESULTS.md` is generated by `mise run suite-bench` from the
# report `test:bats` leaves behind, for the same reason `CHANGELOG.md` is here:
# its generator owns the bytes. Formatting it would mean every regeneration
# lands unformatted, gets rewritten by prettier, and has to be regenerated again
# to stay byte-stable — churn in a file nobody hand-edits.
bench/suites/RESULTS.md

# `bench/rust/RESULTS.md` is the Rust suite's half of that pair, generated by
# `mise run suite-bench-rust` (CLOUD-1208). Same generator-owns-the-bytes reason
# as its shell sibling directly above.
bench/rust/RESULTS.md
