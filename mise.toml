# The single source of truth for this repo's dev environment: every tool, env
# var, and task lives here, so CI, git hooks, and local shells all run the exact
# same thing. Provisioned by mise (https://mise.jdx.dev).
#
#   mise install       # get every tool below at its pinned version
#   mise tasks         # list the tasks
#   mise run ci        # everything CI runs, locally

[tools]
rust = { version = "1.85.0", components = "rustfmt,clippy" }
hk = "1.54.0"                                                # git-hook runner (see hk.pkl)
"aqua:cli/cli" = "2.97"                                      # gh — reads CI checks (repo scope bundles checks-read) and drives PRs; `gh pr checks --watch`
"aqua:EmbarkStudios/cargo-deny" = "0.20"                     # dependency license/advisory policy (deny.toml)
"aqua:anchore/syft" = "1.42"                                 # SBOM of the tagged source: cargo-deny judges that tree, this inventories it (mise-tasks/sbom)
"aqua:release-plz/release-plz" = "0.3"                       # conventional-commit-driven semver + changelog
"aqua:jqlang/jq" = "1.7"                                     # JSON for shell tasks — reads the PreToolUse payload in `gh-guard`
# The measurement instrument for `mise run bench` (CLOUD-207). Batten runs as a
# PreToolUse hook, so its startup cost is paid on every mediated tool call, and
# that number is only defensible if the thing producing it is fixed: a change in
# how the sampler warms, discards outliers, or computes a quantile enters the
# series as a step change indistinguishable from a real regression. Pinned here
# with every other tool, so `mise.lock` carries its per-platform checksums.
#
# `aqua:`, never `cargo:` — batten.toml's `no-source-built-tool` rule refuses a
# tool compiled here, and hyperfine ships prebuilt linux-arm64/x64 and macOS
# assets, so `mise run lock-complete` can hold every platform this repo installs
# on. Deliberately NOT in ci.yml's install lists: nothing on the landing path
# measures (see .github/workflows/bench.yml for why measurement is scheduled).
"aqua:sharkdp/hyperfine" = "1.20.0"
# NOTE: the MSRV gate (`mise run msrv`) deliberately pins NO tool. cargo-msrv
# would be the obvious choice and cannot be used: upstream ships no
# aarch64-unknown-linux binary in ANY release, so a release-asset backend locks
# no `linux-arm64` entry and `mise run lock-complete` rejects it — correctly,
# since an arm64 sandbox could then not provision at all. Its `cargo:` backend
# sidesteps that rule but source-builds a large tree (it pulls the AWS SDK):
# measured 318s here, uncached, on every run. The gate reads the declared floor
# from `cargo metadata` and drives rustup directly instead.
# Coverage as a REPORT, never a gate (CLOUD-111): `mise run coverage`, surfaced
# by a scheduled workflow, deliberately absent from `verify` and from `final`.
# Unlike cargo-msrv above, this one ships aarch64-unknown-linux-gnu, so a
# release-asset backend locks every platform `lock-complete` requires.
"aqua:taiki-e/cargo-llvm-cov" = "0.8.7"
# mise-tasks/ holds real bash programs, so they get the same lint+format
# treatment Rust gets: shellcheck is their clippy, shfmt their rustfmt.
"aqua:koalaman/shellcheck" = "0.11.0"
"aqua:mvdan/sh" = "3.12.0"            # shfmt
# The parallel backend for `test:bats`. The shell suite was 83% of the `ci`
# job's hk gate and ran strictly serially: 247.01s of 298.87s on run
# 31531728503, against a 166s cargo chain beside it (CLOUD-386).
#
# bats' `--jobs` needs GNU parallel or this, and GNU parallel is not choosable
# here: it ships as a source tarball with no aqua/ubi/github-release
# coordinate, so it cannot be a [tools] key at all — and an `apt-get install`
# in ci.yml is the drift `ci-tools-check` exists to refuse. rush is a prebuilt
# Go binary with per-platform release assets, so `lock-complete` can hold every
# platform this repo installs on.
"aqua:shenwei356/rush" = "0.9.0"
"aqua:rhysd/actionlint" = "1.7.7" # workflow mistakes (zizmor covers workflow security)
"aqua:tamasfe/taplo" = "0.10.0"   # TOML lint + format — mise.toml is edited constantly
pkl = "0.32.1"                    # evaluates hk.pkl as a check, not only when hk runs it
# prettier formats the Markdown (AGENTS.md, CONTRIBUTING.md). It is the one
# npm tool here, so node comes with it and nothing else depends on either.
node = "24"
"npm:prettier" = "3.6.2"
zig = "0.16"             # the linker for the macOS release build and the darwin-link gate: zig carries Darwin linker support, so Linux emits a Mach-O with no Apple SDK
# github:, not ubi: — same upstream release assets, but the github backend locks
# a url and a per-platform checksum where ubi locked a bare version and nothing
# else, which made this the one tool installed from an unverified download
# (CLOUD-281). Prebuilt either way, so its own MSRV (1.88) never constrains this
# repo's pinned 1.85.
"github:rust-cross/cargo-zigbuild" = "0.23"
zizmor = "1.29.0"                           # GitHub Actions static analysis; run via `mise run zizmor` (in `verify` and CI)
uv = "0.8"                                  # fast Rust installer the pipx: backend uses to fetch Serena
# Serena's LSP-backed semantic code tools (see .mcp.json). Pinned here — not in
# .mcp.json — so its version lives with every other tool and lands in mise.lock.
# The pipx backend installs it with `uv` (present above), keeping the fast resolver.
"pipx:serena-agent" = "1.6.1"

[env]
CARGO_TERM_COLOR = "always"
# bats comes from the tests/bats submodule, not a mise tool, and this is what
# puts it on PATH. A tool pin would be the obvious choice and is the wrong one:
# the published package is a GitHub *source archive*, and those are not
# byte-stable, so `mise lock` records a checksum a later download legitimately
# fails (CI hit exactly that: "Checksum mismatch ...
# bats-core/archive/refs/tags/v1.14.0.tar.gz"). A submodule pins the commit
# itself, so there is nothing to re-download or re-hash. The rule generalises:
# lock a binary artifact, vendor a source tree. Run `git submodule update
# --init` once per clone.
_.path = ["tests/bats/bin"]
# Conventional Commits subject pattern, shared by the commit-msg hook and the CI
# commit linter so there is exactly one definition of "valid commit".
CONVENTIONAL_RE = '^(build|chore|ci|docs|feat|fix|perf|refactor|revert|style|test)([(][a-z0-9._-]+[)])?!?: .+'
# The checks that carry a verdict about THIS repository, named exactly as GitHub
# names their check-runs (the matrix leg's suffix included). Shared by `ci-wait`
# ("is this SHA green?") and `land`'s `graded_runs` ("does this SHA carry an
# answer yet?") for the same reason CONVENTIONAL_RE is shared: a second copy that
# drifted would put `land` back to waiting on runs that will never be graded.
#
# It exists because "any graded run" is not the question. `SonarCloud Code
# Analysis` and `release-plz` are not draft-gated, so they grade on the draft
# push and satisfied the old predicate by themselves — waving through a set in
# which every check that builds this repository was `skipped`. That landed #261
# on a SHA no CI run of ours ever judged (CLOUD-327).
#
# Hand-maintained until CLOUD-54's derived `[ci].required_checks` lands, and NOT
# on the honour system meanwhile: `mise run ci-local-parity` fails if a name here
# matches no job, or if a `pull_request` job is missing from here.
CI_REQUIRED_CHECKS = "ci,cross,commit-lint,zizmor,darwin-link (aarch64-apple-darwin),msrv,final,action"

# --- GitHub reachability behind an egress proxy (Claude Code web sandbox etc.) ---
# mise resolves every tool's release through GitHub's *API* host, api.github.com.
# When an egress proxy fronts the network (HTTPS_PROXY set) and injects a
# repo-scoped token, that host answers 403 for third-party tool repos and a bare
# `mise install` fails ("GitHub access to this repository is not enabled"). The
# fix is not to give up — GitHub is reachable — it is to send just the API/asset
# hosts straight to GitHub, over the direct route (our tools are public repos, so
# unauthenticated resolution works there; GH_TOKEN below still carries our PAT):
#   * NO_PROXY gains api.github.com (+ the redirect/asset hosts) so those calls
#     bypass the proxy. github.com itself is deliberately left proxied, so git
#     keeps its proxy-provided auth to button-inc/batten. The guard appends once
#     and preserves the existing NO_PROXY (which carries pypi.org/crates so the
#     uv-backed Serena install keeps working). Off-sandbox there is no proxy, so
#     this is a no-op.
# See AGENTS.md, "GitHub is reachable — never report the toolchain as unrunnable".
# `gh` and zizmor read GH_TOKEN. In the sandbox the ambient GH_TOKEN is a proxy
# placeholder that 401s when sent direct (NO_PROXY, above), so set it to our PAT;
# in CI there is no PAT, so fall back to MISE_GITHUB_TOKEN, which mise-action sets
# to the job token. Both are valid, non-empty tokens (never the proxy
# placeholder), so a tool that rejects an *empty* GH_TOKEN — like zizmor — works
# in both. Only these two tokens, per project policy.
GH_TOKEN = "{{ get_env(name='GITHUB_PERSONAL_ACCESS_TOKEN', default=get_env(name='MISE_GITHUB_TOKEN', default='')) }}"
NO_PROXY = "{% set cur = get_env(name='NO_PROXY', default='') %}{% if 'api.github.com' in cur %}{{ cur }}{% else %}api.github.com,objects.githubusercontent.com,codeload.github.com,uploads.github.com{% if cur %},{{ cur }}{% endif %}{% endif %}"
no_proxy = "{% set cur = get_env(name='no_proxy', default=get_env(name='NO_PROXY', default='')) %}{% if 'api.github.com' in cur %}{{ cur }}{% else %}api.github.com,objects.githubusercontent.com,codeload.github.com,uploads.github.com{% if cur %},{{ cur }}{% endif %}{% endif %}"

[tasks.fmt]
description = "Run every fixer over the tree (rustfmt, shfmt, taplo, prettier) — hk owns the step list"
run = "hk fix --all"

[tasks.fix]
description = "Everything `fmt` does, plus clippy's autofixes and the derived artifacts — the one command that empties a messy tree"
shell = "bash -c"
# The symmetric partner to `lint`: where `lint` reports every fixable thing,
# this fixes every one it can. `fmt` stays the formatters-only subset, because
# it is what CONTRIBUTING.md, .claude/rules/toolchain.md and .editorconfig point
# at, and because "reformat my tree" and "rewrite my code and regenerate
# artifacts" deserve different blast radii.
#
# A SEQUENTIAL BODY, deliberately not `depends`. mise runs a `depends` list in
# parallel, and these three stages contend twice over: on the cargo target-dir
# lock, and on the same file bytes — `clippy --fix` rewrites the sources that
# rustfmt then formats. Ordering is the whole point, so it is spelled out:
#
#   1. clippy --fix   rewrites sources
#   2. completions + schema   regenerate from the (now rewritten) surface
#   3. hk fix --all   formats everything the first two produced, last
#
# Step 2 is this repo's "snapshot accept": `completions-check` and
# `schema-check` diff those artifacts byte-for-byte against their generator, so
# regenerating them is exactly what an `insta accept` is elsewhere.
#
# --allow-dirty/--allow-staged because a tree with uncommitted work is the only
# state this task is ever run from; refusing there would make it useless.
#
# Task bodies do not run under `set -e` (.claude/rules/toolchain.md), and a
# half-applied fix that reported success would send the user to commit a tree
# the gates still reject, so the body opts in.
run = '''
set -eu
cargo clippy --fix --allow-dirty --allow-staged --all-targets --all-features
mise run completions
mise run schema
hk fix --all
echo "fix: clippy autofixes applied, derived artifacts regenerated, every fixer run"
'''

[tasks.lint]
description = "Every linter over the whole tree — Rust, TOML and workflows, fanned out"
# Was clippy alone, which made `mise run lint` a claim the repo could not keep:
# the tree is TOML-heavy and carries twelve workflows, and hk gated both while
# the task layer offered no way to run them. The subtasks below are the entry
# points; hk still drives its own file-scoped steps at commit time.
depends = ["lint:clippy", "lint:fmt", "lint:toml", "lint:actions"]

[tasks."lint:clippy"]
description = "Clippy with warnings denied"
# hk's `cargo-clippy` step overrides the builtin command with THIS task, so the
# flags live in one place. The workspace lints only mean anything if every
# target is checked and a warning fails.
# Receipt-gated (CLOUD-424): unchanged sources under an unchanged clippy are a
# verdict already held.
run = '''
if mise run step-receipt check lint:clippy; then exit 0; fi
if ! cargo clippy --all-targets --all-features -- -D warnings; then exit 1; fi
mise run step-receipt record lint:clippy || true
'''

[tasks."lint:fmt"]
description = "rustfmt in check mode"
# Safe as the single definition: hk's `cargo-fmt` builtin already checks the
# whole workspace (`--manifest-path {{workspace_indicator}}`), not file by file,
# so routing it here costs no scoping.
# Receipt-gated (CLOUD-424), same shape as lint:clippy.
run = '''
if mise run step-receipt check lint:fmt; then exit 0; fi
if ! cargo fmt --all --check; then exit 1; fi
mise run step-receipt record lint:fmt || true
'''

[tasks."lint:toml"]
description = "taplo: TOML syntax, duplicate keys, schema bindings, and formatting"
shell = "bash -c"
# TRACKED FILES ONLY, and this is not tidiness. A bare `taplo lint` walks the
# whole tree including `target/`, where the test suite writes deliberately
# corrupt fixtures — `target/tmp/trust-corrupt-config/batten.toml` is literally
# `this is not = = toml`, written on purpose to prove the loader rejects it. A
# gate that fails on its own test data is a gate someone switches off. hk feeds
# its steps `{{files}}` from git for exactly this reason; this is that same set.
#
# `-z` + `xargs -0`, and NOT a bash array: mise renders task bodies through
# Tera, which reads the `{#` in `${#files[@]}` as a comment-open and fails the
# task with "Closing comment tag `#}` not found" before a shell ever sees it.
# NUL-delimited is the more robust idiom anyway. `-r` so an empty set is a
# no-op rather than a bare `taplo lint` that would walk the tree after all.
run = '''
set -euo pipefail
git ls-files -z '*.toml' | xargs -0 -r taplo lint
git ls-files -z '*.toml' | xargs -0 -r taplo format --check --diff
echo "lint:toml: every tracked TOML file parses, lints and is formatted"
'''

[tasks."lint:actions"]
description = "actionlint: workflow mistakes (bad `on:` keys, undefined matrix refs, shellcheck over `run:` blocks)"
# Complements rather than duplicates `zizmor`, which audits workflow SECURITY.
# Bare invocation: actionlint defaults to .github/workflows, and unlike taplo it
# does not walk the tree, so there is nothing to scope away.
run = "actionlint"

[tasks.coverage]
description = "Report: line/region coverage of the Rust suite. A REPORT, never a gate — deliberately not in `verify` and not a required check"
shell = "bash -c"
# WHY THIS IS NOT A GATE, and must not become one. A coverage-percentage
# threshold rewards line-touching over behaviour assertion: the cheapest way to
# move the number is a test that executes code and asserts nothing, which is
# strictly worse than no test because it also has to be maintained. The real
# quality bar here is the contract suite — byte-stable snapshots, negative exit
# codes, the derived-artifact diffs — and those already fail loudly. So this
# emits a number for a human to read and draws no conclusion from it.
#
# Consequences of that, all deliberate: absent from `[tasks.verify]`, absent
# from `final`'s `needs` in ci.yml, and its workflow runs on a schedule rather
# than on `pull_request` — which is also what keeps `ci-local-parity` honest,
# since that gate requires every task a PR workflow runs to be one `verify`
# runs. Wiring coverage into `verify` to satisfy it would be the gate this
# refuses to be.
#
# `llvm-tools-preview` is NOT added to the `rust` pin's `components` above: that
# would make every CI job download it for a tool only this task uses. Installed
# here, on demand, and only when missing — the same "don't mutate the toolchain
# on the common path" reasoning as `[tasks.msrv]`.
#
# Task bodies do not run under `set -e`, and a report built from a half-run
# suite is a misleading number rather than a missing one, so the body opts in.
run = '''
set -euo pipefail
if ! rustup component list --installed | grep -qx 'llvm-tools-preview'; then
	if ! rustup component add llvm-tools-preview; then
		echo "::error:: coverage: could not install llvm-tools-preview, so no report was produced." >&2
		exit 1
	fi
fi
mkdir -p "${COVERAGE_OUT_DIR:-target/coverage}"
# Two artifacts from one instrumented run: lcov for tooling that consumes it,
# and a summary a human reads. `--workspace` matches what `test:cargo` runs.
cargo llvm-cov --workspace --lcov --output-path "${COVERAGE_OUT_DIR:-target/coverage}/lcov.info"
cargo llvm-cov report --summary-only
'''

[tasks.test]
description = "Run every test suite — Rust and shell"
depends = ["test:cargo", "test:bats"]

[tasks."test:cargo"]
description = "Run the Rust workspace test suite"
# Routed through the per-step receipt (CLOUD-424): same sources, same command,
# same toolchain ⇒ same verdict, so a hit answers from the receipt instead of
# rebuilding the workspace. Fail closed — a check that cannot answer runs the
# suite — and the record is an economy whose failure must never fail a suite
# that passed.
run = '''
if mise run step-receipt check test:cargo; then exit 0; fi
if ! cargo test --workspace; then exit 1; fi
mise run step-receipt record test:cargo || true
'''

[tasks.msrv]
description = "Gate: the workspace still builds on the `rust-version` it declares"
shell = "bash -c"
# VERIFY the declared floor, never SEARCH for the true one. A search (what
# `cargo msrv find` does) is a compile per candidate version and answers a
# question about the world; this asks the one question that is a property of
# this commit — does the number we publish still hold.
#
# WHY THIS IS NOT ALREADY COVERED by the `ci` job's `cargo check`. The pinned
# toolchain equals the declared MSRV *today*, which makes the coverage look
# redundant. They are two independent numbers: bump `[tools] rust` to 1.95 and
# leave `rust-version = "1.85"`, and every other gate compiles happily on 1.95
# while the published claim quietly becomes false. That is CLOUD-271's shape
# exactly. This task reads the DECLARED number and builds with THAT compiler, so
# the two can never silently diverge.
#
# Read via `cargo metadata`, not by grepping a manifest: the crate inherits with
# `rust-version.workspace = true`, so its own file does not carry the number and
# the root is a virtual workspace. `sort -V | tail -n1` takes the highest
# declared floor, which is the workspace's effective one.
#
# Task bodies do not run under `set -e` (.claude/rules/toolchain.md), and every
# line here changes the verdict, so the body opts in explicitly.
run = '''
set -euo pipefail
# Receipt-gated (CLOUD-424): the declared floor lives in Cargo.toml and the
# host toolchain is in the key, so a bump to either re-derives.
if mise run step-receipt check msrv; then exit 0; fi
msrv=$(cargo metadata --format-version 1 --no-deps |
	jq -r '[.packages[].rust_version | select(. != null)] | unique | .[]' |
	sort -V | tail -n1)
if [ -z "$msrv" ]; then
	echo "::error:: msrv: no package declares rust-version, so there is no floor to verify." >&2
	exit 1
fi
# Install only when absent. rustup has no cross-process lock (CLOUD-220), and
# the cheapest way not to race one is not to mutate the toolchain dir on the
# common path — where the declared floor equals the pinned toolchain, this is a
# no-op. `--profile minimal` because a type-check needs no docs or clippy.
if ! rustup toolchain list | grep -qE "^${msrv}(\.|-)"; then
	if ! rustup toolchain install "$msrv" --profile minimal; then
		echo "::error:: msrv: could not install the declared toolchain $msrv, so the floor is unverified." >&2
		exit 1
	fi
fi
# The shipped surface (lib + bins), which is what an MSRV claim is *about*: a
# consumer on the floor compiles our crate, not our test harness. `--all-targets`
# would drag dev-dependencies' own floors into a claim we do not make about them.
cargo "+$msrv" check --workspace
mise run step-receipt record msrv || true
echo "msrv: the workspace builds on its declared rust-version ($msrv)"
'''

[tasks.deny]
description = "cargo-deny: licenses, advisories, sources"
# Receipt-gated (CLOUD-424) on the dependency set and cargo-deny's own version.
# The advisory DB is a property of the world, and worlds change on a clock, not
# on this branch: CI still runs the full check on every PR, which is the same
# split lock-currency already made.
run = '''
if mise run step-receipt check deny; then exit 0; fi
if ! cargo deny check; then exit 1; fi
mise run step-receipt record deny || true
'''

[tasks.batten-check]
description = "Consumer #1: evaluate the committed batten.toml with batten's own engine against this repository"
# `cargo run` rather than an installed binary, so the gate always judges the
# working tree's engine and config together — the pair that ships.
# Receipt-gated (CLOUD-424); the key's specs mirror the hk step's glob, which
# is the union of what `batten check` actually reads.
run = '''
if mise run step-receipt check batten-check; then exit 0; fi
if ! cargo run --quiet -p batten -- check; then exit 1; fi
mise run step-receipt record batten-check || true
'''

[tasks.policy-budget]
description = "Gate: the always-loaded context (AGENTS.md + anything else declared always-load) fits its token budget"
# The successor to `mise-tasks/context-budget`, deleted with CLOUD-50. The whole
# predicate — which files are counted, what is stripped before counting, and both
# thresholds — is now `[budget.instructions]` in batten.toml, so the gate is
# config a reviewer can read rather than arithmetic buried in a shell script.
# `cargo run` for the same reason `batten-check` uses it: the gate judges the
# working tree's engine and config together, the pair that ships.
run = "cargo run --quiet -p batten -- policy budget"

[tasks.completions]
description = "Regenerate the committed shell completions from the command surface"
# The binary emits on stdout only, so `generate` stays a read-effect verb (§5)
# and the redirect that refreshes the committed artifact is here, in the caller.
# `cargo run` rather than an installed binary, so the artifact always tracks the
# working tree's surface — the pair `completions-check` then diffs.
shell = "bash -c"
run = '''
for shell in bash zsh fish; do
  cargo run --quiet -p batten -- generate completions --shell "$shell" >"completions/batten.$shell"
done
echo "completions: regenerated for bash, zsh, fish"
'''

[tasks.schema]
description = "Regenerate the committed JSON Schemas for both config surfaces from the config types"
# Same shape as `completions`: the binary emits on stdout only, so `generate`
# stays a read-effect verb (§5) and the redirect lives here, in the caller.
#
# Two surfaces, two artifacts (CLOUD-239): `batten.toml` is the committed
# authority, `batten.local.toml` the raise-only override that accepts a strict
# subset. Both are regenerated together so neither can be refreshed alone and
# drift while the other looks current.
run = '''
set -eu
cargo run --quiet -p batten -- generate schema --surface authority >schema/batten.schema.json
cargo run --quiet -p batten -- generate schema --surface override >schema/batten.local.schema.json
'''

[tasks.zizmor]
description = "Static-analyse the GitHub Actions workflows (dangerous triggers, unpinned actions, injection sinks)"
# Same tool + severity CI enforces. GH_TOKEN (set in [env]) enables the online
# audits. This is the one command CI's zizmor job also runs, so they cannot drift.
#
# `action.yml` is named alongside `.github/` because the shipped Action (CLOUD-68)
# is a workflow surface with the same injection and unpinned-uses hazards, and
# zizmor's directory walk does not reach a repository-root action definition.
# Receipt-gated (CLOUD-424) on the workflow surface and zizmor's version.
run = '''
if mise run step-receipt check zizmor; then exit 0; fi
if ! zizmor --min-severity high .github/ action.yml; then exit 1; fi
mise run step-receipt record zizmor || true
'''

[tasks.lock-check]
description = "Currency only: would regenerating mise.lock change it? Runs on a schedule, never on the landing path — it fetches and it writes"
shell = "bash -c"
# What this actually answers is "has upstream changed since this commit", which
# is a property of the world, not of the branch. It costs a remote round trip
# per tool and REWRITES the tracked lockfile, so it belongs on a schedule
# (.github/workflows/lock-currency.yml), not in `verify`.
#
# It does NOT answer completeness, though it used to claim to. Regenerate-and-
# diff detects drift only: `mise lock` never removes or repairs an existing
# entry, so a lockfile that is stably wrong passes forever. One did — a
# cargo-zigbuild platform key carrying a checksum and no url, exactly the
# partial entry this comment used to cite as the motivation. `mise run
# lock-complete` asks that question of the committed bytes instead.
#
# The old comment also asserted "CI installs with `mise install --locked`".
# It does not, and never did — no workflow passes --locked.
run = '''
# Fails closed: mise task bodies do not run under `set -e`, so an unguarded
# `mise lock` that failed would leave mise.lock untouched, the diff below clean,
# and this gate reporting "complete and current" about a file it never
# regenerated — the same silent false-green class as the fetch in linear-check.
# The single sanctioned lockfile writer, so it says so. Measured on mise
# 2026.8.2: `mise lock` writes regardless of `[settings] lockfile`, so this
# assignment is not what makes the regeneration work — it is the declaration
# that this task, alone in the repo, is allowed to write the file that
# `[settings] lockfile = false` denies to everything else (CLOUD-223). It also
# holds the currency job upright if a later mise gates `lock` on the setting.
if ! MISE_LOCKFILE=true mise lock; then
  echo "::error:: \`mise lock\` failed, so the lockfile could not be regenerated and its currency is unverified. Refusing to pass." >&2
  exit 1
fi
if ! git diff --quiet -- mise.lock; then
  echo "::error:: mise.lock was stale/incomplete — regenerated it. Review and commit the change; CI installs with --locked and needs complete per-platform URLs." >&2
  git --no-pager diff --stat -- mise.lock >&2
  exit 1
fi
echo "lock-check: mise.lock is complete and current"
'''

[tasks.cross-check]
description = "Type-check for other targets from Linux (no macOS runner needed)"
# The `rustup target add` below is not idempotent against a half-installed
# component; `doctor` is what makes it one.
depends = ["doctor"]
# The Darwin triples are NOT here: they get a stronger check — a real link — in
# `darwin-link`, which runs as its own parallel CI job. Checking them here too
# would be duplicate work on the critical path.
run = '''
# Receipt-gated (CLOUD-424): a hit skips the target install too — the verdict
# is about the sources, and the toolchain half lives in the key.
if mise run step-receipt check cross-check; then exit 0; fi
for t in x86_64-pc-windows-gnu; do
  # Guarded because task bodies do not run under `set -e`: with more than one
  # triple in the list, a failed target install would otherwise be masked by a
  # later iteration's exit status and this gate would pass having checked less
  # than it claims. Routed through target-ensure so a concurrent toolchain
  # mutator queues on the lock instead of colliding inside rustup (CLOUD-220).
  if ! mise run target-ensure "$t"; then
    echo "::error:: could not install target $t, so its type-check did not run." >&2
    exit 1
  fi
  # `check` does codegen-to-metadata without linking, so it needs no target
  # linker or SDK — cross-platform compile coverage at Linux-runner cost.
  #
  # `--all-targets` because the default target set is lib and bins ONLY: no
  # `#[cfg(test)]` module, nothing under `tests/`. So this gate carried the
  # repository's cross-platform claim while never once type-checking the ~440
  # integration tests against a foreign triple — and the test code is the half
  # most likely to break, since a suite reaches for `PermissionsExt`, `#!/bin/sh`
  # fixtures and Unix paths as a matter of course while the library's
  # platform-specific code is deliberate and already `#[cfg(unix)]`-gated. A test
  # using a Unix-only API without that gate compiled fine here forever
  # (CLOUD-395). It passes today, which is the point of adding the flag now: the
  # property held by luck, and a mechanism is what makes it hold on purpose.
  if ! cargo check --workspace --all-targets --target "$t"; then
    echo "::error:: cross-check: $t does not type-check." >&2
    exit 1
  fi
done
mise run step-receipt record cross-check || true
'''

[tasks.hk-version]
description = "Gate: hk's version is pinned identically in mise.toml and hk.pkl (amends URL). A bump to one without the other is the moment to revisit the hook config."
shell = "bash -c" # uses grep -oE + cut; keep off /bin/sh for consistency
run = '''
mise_v=$(grep -E '^hk = ' mise.toml | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | head -n1)
pkl_v=$(grep -oE 'hk@[0-9]+\.[0-9]+\.[0-9]+' hk.pkl | head -n1 | cut -d@ -f2)
if [ "${mise_v:?}" != "${pkl_v:?}" ]; then
  echo "::error:: hk version drift: mise.toml pins $mise_v but hk.pkl amends $pkl_v." >&2
  echo "Sync both, then revisit hk.pkl for new features worth adopting (see AGENTS.md)." >&2
  exit 1
fi
echo "hk-version: mise.toml and hk.pkl agree on hk $mise_v"
'''

[tasks.no-docs-tree]
description = "Gate: no committed docs/ tree. Research deliverables and evidence notes attach to the Linear issue, not a repo docs/ folder (see AGENTS.md)."
shell = "bash -c"
run = '''
tracked=$(git ls-files 'docs/*' 'docs/**' | head -n1)
if [ -n "$tracked" ]; then
  echo "::error:: committed docs/ tree found (e.g. $tracked). Research evidence belongs on the Linear issue, not in a repo docs/ folder (see AGENTS.md)." >&2
  exit 1
fi
echo "no-docs-tree: no committed docs/ tree"
'''

[tasks."test:bats"]
description = "Run the shell test suite (tests/*.bats) — cargo test for the mise-tasks/ programs"
# bats comes from a submodule, not [tools], so a fresh clone has no bats and this
# task reports "bats: not found" — which reads as a missing TOOL and sends the
# reader to mise. `doctor` checks out the submodule instead. Only the submodule
# half: this dependency re-runs doctor in whatever process invokes test:bats,
# and when that is hk's test:bats step it is a CHILD mise process — task-node
# dedup is per-process, so a full doctor here raced the outer DAG's doctor
# inside `rustup target add` and both rolled back (CLOUD-220). The targets are
# owned by the tasks that need them (cross-check, darwin-link), each routed
# through the target-ensure lock.
depends = ["doctor --no-targets"]
# Parallel across files, never within one (CLOUD-386). Serial, this suite was
# the pole of the `ci` job — 247.01s of 298.87s, with the cargo chain beside it
# finishing at 166s — and `verify` pays it again on every `land` lap, so it sat
# in the exponent of the lap economics twice.
#
# `$(nproc)` rather than a literal: the number that is right for today's
# ubuntu-latest is wrong the moment a runner changes size, and a stale literal
# reads as deliberate. Across-files is already enough — the largest file is 49
# of 800 tests, so the schedule is not tail-bound — and it is the half that is
# safe by construction: within-file parallelism would interleave a file's own
# setup/teardown with its cases, which is exactly what the suites that model
# lock and race behaviour must not have done to them.
# Receipt-gated (CLOUD-424). Deliberately glob-less in step-receipt's table:
# these suites read mise.toml, hk.pkl, workflows and fixture trees all over the
# repo, so the honest input set is the whole tracked tree — a rebase that
# changed no bytes still skips, and any content change anywhere re-runs.
run = '''
if mise run step-receipt check test:bats; then exit 0; fi
if ! bats --print-output-on-failure --parallel-binary-name rush --jobs "$(nproc)" --no-parallelize-within-files tests/*.bats; then exit 1; fi
mise run step-receipt record test:bats || true
'''

[tasks.hooks]
description = "Run the git-hook gate (fmt, lint, test) through hk — the same hk.pkl the pre-commit hook uses, in check-only mode"
run = "hk check --all"

[tasks.ci]
description = "Everything CI runs"
# fmt-check + lint + test come from the SAME hk.pkl the pre-commit hook runs
# (via `hooks`), so CI exercises the hook config itself; `deny` rounds it out.
depends = ["hooks", "deny"]

[tasks.verify]
description = "Full local pre-flight — run this before marking any draft PR ready"
shell = "bash -c"
# lock-check is deliberately NOT here. It runs `mise lock` — a remote round
# trip that rewrites the tracked lockfile — so its verdict is "did upstream
# change since this commit", a property of the world rather than of the branch.
# On the landing path that fails a change for drift it did not cause, and
# leaves the mutation in the tree for the next `git add -A` to sweep up. The
# completeness question it was named for is now `lock-complete`, a pure gate in
# hk.pkl; currency runs on a schedule (.github/workflows/lock-currency.yml).
#
# `tree-clean` is here AND in the body, and the duplication is the design
# (CLOUD-277). Here it fails a dirty tree in seconds rather than after the ~170s
# the rest of this list costs. In the body it is load-bearing: `depends` runs
# before the tree can be dirtied MID-RUN, which is exactly the measured
# incident — a backgrounded `land` verifying while the session edited the next
# ticket in the same worktree. One authority, asserted at both ends of the
# window, the same shape `linear-check` already has in `verify` and
# `ready-guard`. It is deliberately NOT an hk step: `pre-commit` runs over a
# tree that is dirty by definition.
depends = ["tree-clean", "ci", "cross-check", "darwin-link", "zizmor", "msrv"]
run = '''
# A task body does not run under `set -e` (see .claude/rules/toolchain.md), so
# every step whose failure changes the verdict is guarded by hand. Unguarded,
# a failed linear-check let this body run commit-lint against a stale
# origin/main, write the receipt below, and print "fast-forward-green" — a
# silent false green in the one gate that decides whether CI minutes get spent.
# The code is PROPAGATED, not flattened (CLOUD-318). `linear-check` answers 2
# for "measured, and this branch is behind" and 1 for the two environment
# refusals — a fetch that failed, an origin/main that does not resolve. A caller
# driving a loop laps on the first and stops on the second, and it can only do
# that if the distinction survives this guard. Everything below stays 1: a
# content failure is a stop wherever it comes from.
# First, because it decides whether anything below is ABOUT the commit it names.
# Every gate above read the working tree; the receipt at the bottom is keyed to
# HEAD. A dirty tree that FAILS is loud and self-correcting; one that PASSES
# writes a receipt `verified` matches and `ready-guard` accepts, so CI runs a
# commit that was never the thing that passed (CLOUD-277).
if ! mise run tree-clean; then
  echo "::error:: verify: the working tree is not the bytes at HEAD, so a receipt naming HEAD would attest a run of something else. No receipt written." >&2
  exit 1
fi
linear_rc=0
mise run linear-check || linear_rc=$?
if [ "$linear_rc" = 2 ]; then
  echo "::error:: verify: main moved under this branch — rebase and verify again, there is nothing here to fix. No receipt written." >&2
  exit 2
fi
if [ "$linear_rc" != 0 ]; then
  echo "::error:: verify: linear-check could not establish linearity at all — see its message. No receipt written." >&2
  exit 1
fi
if ! BASE_SHA="$(git rev-parse origin/main)" HEAD_SHA="$(git rev-parse HEAD)" mise run commit-lint; then
  echo "::error:: verify: commit-lint failed. No receipt written." >&2
  exit 1
fi
# The receipt write is the binary's job (CLOUD-203): `batten receipt record`
# keys the claim to the exact commit verified — an amend or a rebase produces a
# new HEAD and therefore no receipt, which is the point (see ready-guard) — and
# maintains the grandfathered $GIT_DIR/batten-receipts/ compatibility layout
# those readers consume. Guarded like every step above: a receipt that cannot
# be recorded must fail verify and leave no receipt.
if ! cargo run --quiet -p batten -- receipt record verify; then
  echo "::error:: verify: could not record the receipt. No receipt written." >&2
  exit 1
fi
echo "verify: fast-forward-green — rebased on latest main, ci + cross + commit-lint all pass"
'''

[tasks.commit-msg]
description = "Validate one commit-message file is Conventional Commits"
run = '''
head -n1 "{{arg(name="file")}}" | grep -Eq "$CONVENTIONAL_RE" \
  || { echo 'commit-msg: subject must follow Conventional Commits, e.g. "feat(cli): add check subcommand"'; exit 1; }
'''

[tasks.commit-lint]
description = "Validate every commit in BASE_SHA..HEAD_SHA is Conventional Commits"
shell = "bash -c" # uses ${sha:0:8}; mise defaults tasks to /bin/sh (dash)
run = '''
fail=0
for sha in $(git rev-list --no-merges "${BASE_SHA:?}..${HEAD_SHA:?}"); do
  subject=$(git show -s --format=%s "$sha")
  if printf '%s' "$subject" | grep -Eq "$CONVENTIONAL_RE"; then
    echo "OK    ${sha:0:8}  $subject"
  else
    echo "::error::Non-conventional commit ${sha:0:8}: '$subject'"
    fail=1
  fi
done
exit $fail
'''

[tasks.release-pr]
description = "Open/update the release PR (version bump + changelog)"
# release-plz derives the next version by diffing each crate against its last
# *published* release, downloaded from a cargo registry. This workspace is
# `publish = false` and never reaches a registry, so that download fails
# ("Package batten not found") and release-plz can never bump past the initial
# version — every run reports "next version is 0.0.0". The documented fix for
# unpublished crates is to supply the baseline as a checkout of the latest
# release tag via --registry-manifest-path. With no tag yet (first release),
# pass nothing and let release-plz treat it as the initial version.
run = '''
set -eu
base_args=""
tag="$(git describe --tags --match 'v[0-9]*' --abbrev=0 2>/dev/null || true)"
if [ -n "$tag" ]; then
  worktree="$(mktemp -d)"
  git worktree add -q --detach "$worktree" "$tag"
  trap 'git worktree remove --force "$worktree" >/dev/null 2>&1 || true' EXIT
  base_args="--registry-manifest-path $worktree/Cargo.toml"
fi
release-plz release-pr --git-token "${GITHUB_TOKEN:?}" $base_args
'''

[tasks.release]
description = "Tag and publish a GitHub release when the version changed"
run = "release-plz release --git-token ${GITHUB_TOKEN:?}"

[settings]
# OFF, so that installing is never a write (CLOUD-223). With it on, any cold
# `mise install` appends install residue to the tracked lockfile — a
# `platforms.<platform>-<exe>` key carrying a checksum and no url, which `mise
# lock` can neither produce nor repair and `mise run lock-complete` rejects by
# name. The session-start hook was taught to install with MISE_LOCKFILE=false,
# and that fixed exactly one caller: the sandbox's own provisioning runs before
# the hook, so a session still opened on a dirty tree. A per-caller opt-out
# cannot cover callers this repo does not own.
#
# Measured, cold data/cache/state dirs, this tool alone: with the setting true a
# `mise install` grew the lockfile by 163 bytes and produced exactly one
# `linux-x64-cargo-zigbuild` key; with it false the file was byte-identical
# afterwards and the key was absent.
#
# Inverting it does: the default is now "no writer", and the one writer that
# should touch the lockfile opts in — `[tasks.lock-check]` sets MISE_LOCKFILE=true
# for its `mise lock`, on the schedule that owns currency
# (.github/workflows/lock-currency.yml), against a disposable checkout. Nothing
# on the landing path writes at all. Gated by `mise run lock-complete`, which
# fails if this is ever set back to true.
lockfile = false
