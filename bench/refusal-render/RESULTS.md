# What a refusal costs the context it lands in

Generated by `mise run refusal-render-bench` at batten **0.0.152**. A REPORT, never a gate: no task in `verify` or the hk gate reads it, and it exits 0 whatever the numbers say.

`Cold` and `Warm` are benchmark INPUTS, not observations. Nothing in this repository detects residency — there is no sightings store, no compaction signal and no harness epoch — so a successor that wants to act on residency must name its own explicit authority for it.

Characters are what is emitted. The token column is `budget::estimate_tokens`, the estimator `policy-budget` already holds instruction files to, rather than a new approximation minted for this table.

**The declared `[refusal] max_tokens` is 24, and it is an input to every `emitted` figure below.** `deny_text` drops the carried routes when they take the line over that bound, so a first sighting is not automatically longer than a repeat in this repository. The `unbounded` columns are the same arm rendered with no ceiling — what the sighting would cost if the budget permitted it.

## `branch write unsafe` (rule `leased-push`)

| strategy               | residency | first sighting | emitted characters | emitted tokens | unbounded characters | unbounded tokens |
| ---------------------- | --------- | -------------- | ------------------ | -------------- | -------------------- | ---------------- |
| `Current`              | `Cold`    | true           | 31                 | 7              | 156                  | 40               |
| `Current`              | `Warm`    | false          | 31                 | 7              | 31                   | 7                |
| `FullEveryTime`        | `Cold`    | true           | 31                 | 7              | 156                  | 40               |
| `FullEveryTime`        | `Warm`    | true           | 31                 | 7              | 156                  | 40               |
| `FirstFullThenCompact` | `Cold`    | true           | 31                 | 7              | 156                  | 40               |
| `FirstFullThenCompact` | `Warm`    | false          | 31                 | 7              | 31                   | 7                |

## `tool run loose` (rule `no-tool-substitution`)

| strategy               | residency | first sighting | emitted characters | emitted tokens | unbounded characters | unbounded tokens |
| ---------------------- | --------- | -------------- | ------------------ | -------------- | -------------------- | ---------------- |
| `Current`              | `Cold`    | true           | 35                 | 8              | 35                   | 8                |
| `Current`              | `Warm`    | false          | 35                 | 8              | 35                   | 8                |
| `FullEveryTime`        | `Cold`    | true           | 35                 | 8              | 35                   | 8                |
| `FullEveryTime`        | `Warm`    | true           | 35                 | 8              | 35                   | 8                |
| `FirstFullThenCompact` | `Cold`    | true           | 35                 | 8              | 35                   | 8                |
| `FirstFullThenCompact` | `Warm`    | false          | 35                 | 8              | 35                   | 8                |

## The margin

- **`branch write unsafe`** — a warm repeat emits 31 characters (7 tokens) today against 31 (7 tokens) delivered in full every time: **0 characters saved per repeat firing**. Unbounded, the same comparison is 31 against 156: **125 characters** — what a residency protocol would have to deliver, and withhold, per firing.
- **`tool run loose`** — a warm repeat emits 35 characters (8 tokens) today against 35 (8 tokens) delivered in full every time: **0 characters saved per repeat firing**. Unbounded, the same comparison is 35 against 35: **0 characters** — what a residency protocol would have to deliver, and withhold, per firing.

**A zero in the emitted column is a measurement, not a gap in the table**, and the two classes reach it by different routes. `tool run loose` declares a `document` route and no `command` route, and a first sighting appends command routes only — so it renders the identical line cold and warm however much budget there is. `branch write unsafe` declares two command routes and WOULD render them, but the carried line exceeds the declared ceiling and falls back to the compact form. The first is a renderer gap; the second is a budget decision. A table without the unbounded columns reports them as the same number and they are not the same finding.

## What is NOT priced here, and why it is not an omission

- **Provider-side prefill and KV-cache cost.** Those are the serving work a request does; this measures logical occupancy of the agent's active context, which survives caching and is what shortens a run. One number serving both would be honest about neither.
- **Whether a shorter refusal produces a better outcome.** That is a paired agent trial (CLOUD-1117), not a rendering measurement, and this table deliberately claims no completion effect.
- **A tokenizer.** The token column is the declared estimator, named above; a provider's real tokenization is not available here and inventing one would be an approximation under a more precise-looking label.
